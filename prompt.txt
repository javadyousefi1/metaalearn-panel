course session have an upload api we should change it
this is curl

curl -X 'POST' \
  'http://185.79.97.146:5000/CourseSession/Upload' \
  -H 'accept: */*' \
  -H 'Content-Type: multipart/form-data' \
  -F 'file=@filldata.png;type=image/png'
  with CourseSessionId too

and enum
public enum CourseSessionUploadType
{
    Video = 1,
    File = 2,
    VideoCover = 3
}


this is file src/pages/course/[id]/Sessions/CourseSessionsPage.tsx and modal src/pages/course/[id]/Sessions/CourseSessionModal.tsx

we have 2 filed
لینک ویدیو
لینک فایل
we should replace them with that api with related enum