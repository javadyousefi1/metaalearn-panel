we want add new section named معرفی دوره  and itr is new segment in course like fgaq and session
in updater course backenbd added 2 feileds
  "shortText": "string",
  "fullText": "string",

 it should be 2 input
 first the antd input
 the second should be richtexteditor
 beacouse user can write and image for intrtuce the course

 we have course update custom hook and have getby id custom hook the default value shorttext and full text is init
 ask quertystion if you have